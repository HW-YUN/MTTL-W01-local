# 보안 안내

이 프로젝트는 MTTL-W01 을 제조사 클라우드 없이 로컬에서 제어하기 위해, 기기가
신뢰하는 TLS 종단을 로컬 릴레이로 옮깁니다. 그 과정에서 만들어지는 신뢰 경계와
주의사항을 정리합니다.

이 문서는 일반적인 보안 주의 안내이며, 보증이나 감사 결과가 아닙니다.

---

## 로컬 신뢰 경계 (trust boundary)

로컬 릴레이는 사실상 **로컬 MITM** 성격입니다. 기기가 로컬에서 만든 Root CA 를
신뢰하도록 만든 뒤, 릴레이가 그 CA 로 서명한 인증서로 기기 쪽 TLS 를 종단합니다.

- 이 신뢰 경계는 **릴레이 호스트와 기기가 있는 네트워크 세그먼트 안에서만** 유효해야
  합니다.
- Root CA 개인키를 가진 쪽은 임의의 도메인에 대해 유효해 보이는 인증서를 만들 수
  있습니다. 키 관리가 이 구조의 핵심입니다.

## Root CA / 개인키 관리

`relay/gen_local_ca.sh` 가 만드는 Root CA 와 leaf 인증서의 개인키는:

- `relay/certs/` 아래에 생성되며 파일 권한은 `600` 입니다.
- **릴레이 호스트에만** 두어야 합니다. 다른 곳으로 복사하지 마세요.
- 저장소에 커밋하면 안 됩니다.
- 유출되었다면 즉시 폐기하고 CA 를 다시 생성한 뒤, 기기에 새 CA 를 다시 배포해야
  합니다.

## 로컬 HTTPS 종단

- `:443` 은 릴레이의 leaf 인증서로 TLS 를 종단합니다.
- `:80` 은 평문입니다. 기기가 `GET /mef/cert/http/pem` 으로 Root CA 를 받아가기
  위한 경로입니다. `:80` 을 기기 세그먼트 밖으로 노출하지 마세요.

## MQTT observer 는 localhost 전용

릴레이의 observer 포트(`:9883`)는 대시보드와 HA 브리지가 상태를 읽고 명령을
주입하는 **평문 MQTT** 경로입니다.

- `127.0.0.1` 바인딩을 유지하세요.
- 외부에 노출되면 인증 없이 콘센트를 제어하거나 텔레메트리를 열람할 수 있습니다.

## DNS 리다이렉트의 영향 범위

DNS 리다이렉트는 `mef.onem2m.uplus.co.kr` / `brk2.onem2m.uplus.co.kr`(그리고
선택적으로 QMS 호스트)만 릴레이로 향하게 합니다.

- 이 리다이렉트를 적용한 resolver 를 사용하는 **다른 기기에도 영향**을 줍니다.
- 기기 전용 resolver 또는 전용 네트워크 세그먼트에서 적용하는 것을 권장합니다.
- 자세한 내용과 롤백은 [network.md](docs/network.md) 를 참고하세요.

## 클라우드 아웃바운드 차단 권장

기기가 있는 네트워크 세그먼트의 아웃바운드 인터넷을 차단하는 것을 권장합니다.
릴레이는 decloud 모드로 동작하며 어떤 요청도 외부로 전달하지 않습니다.

## 대시보드 제어 활성화 위험

대시보드의 콘센트 제어는 **기본적으로 비활성**입니다.

- `--enable-control` 을 붙이면 `POST /api/control` 이 릴레이 observer 로 명령을
  publish 하여 실제 콘센트를 토글합니다.
- 활성화 전에 자신의 기기에서 프로토콜을 직접 확인하고, 첫 토글을 눈으로 관찰하며
  진행하세요.

## OTA 위험

펌웨어 OTA 는 되돌리기 어려운 작업입니다. 잘못된 대상이나 잘못된 이미지는 기기를
손상시킬 수 있습니다. OTA 규칙은 단일 대상 / fail-closed / deny-first 로 설계되어
있지만, 위험 자체가 사라지는 것은 아닙니다. [firmware-ota.md](docs/firmware-ota.md)
를 반드시 먼저 읽으세요.

## 민감정보를 공개 이슈에 올리지 마세요

이 저장소의 GitHub Issue 는 **공개될 수 있습니다.** 이슈·PR·로그·스크린샷·패킷
캡처에 다음을 넣지 말고, 붙여넣기 전에 마스킹하세요.

- 실제 MAC 주소
- 실제 MQTT client_id
- 기기 / 릴레이의 사설 IP
- 비밀번호·토큰 등 시크릿
- 인증서 / 개인키
- 진단 로그 등 민감 로그

## 보안 문제 제보

일반적인(비민감) 문제 유형은 공개 이슈에서 공유할 수 있습니다. 다만 다음에
해당하는 보안 제보는 공개 이슈에 올리지 마세요.

- 위에 나열한 민감정보가 포함되어야 설명이 되는 경우
- 구체적인 공격 방법 / 재현 절차 / 동작하는 익스플로잇이 필요한 경우

이런 경우에는 문제의 성격(class of problem)만 기술하고, 단계별 재현·추출 방법은
쓰지 마세요.

> 현재 이 프로젝트는 별도의 비공개 보안 신고 채널을 제공하지 않습니다. 위
> 기준에 걸리는 제보는 상세를 공개하지 않는 선에서 공유해 주세요.

## 이 문서에 포함하지 않는 것

리버스 엔지니어링 상세, 패킷 캡처 / 디스어셈블 결과, 개인 실험 환경 정보는 이
문서에 포함하지 않습니다.

---

[← README](README.md)
